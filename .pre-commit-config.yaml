repos:
  - repo: local
    hooks:

      - id: isort
        name: isort
        entry: isort
        language: system
        files: ^(aybolit|tests)/.+\.py$

      - id: black
        name: black
        entry: black
        language: system
        files: ^(aybolit|tests)/.+\.py$

      - id: flake8
        name: flake8
        entry: flake8
        language: system
        files: ^(aybolit|tests)/.+\.py$

      - id: mypy
        name: mypy
        entry: mypy
        language: system
        files: ^(aybolit)/.+\.py$

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 'v4.3.0'
    hooks:
      - id: check-added-large-files
      - id: detect-private-key
      - id: check-json
      - id: check-toml
      - id: check-yaml
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: name-tests-test
        files: ^(tests)/.+\.py$
        exclude: ^(tests).+(mock|util|const|fixtur).*(\.py)$
        args:
          - '--pytest-test-first'
      - id: trailing-whitespace
